import { ascending, descending, max, min, range, sum } from "d3-array";
import { hsl, rgb } from "d3-color";
import { format } from "d3-format";
import { interpolateHsl, interpolateHslLong } from "d3-interpolate";
import { select, selectAll } from "d3-selection";

export default {
  range,
  sum,
  ascending,
  descending,
  max,
  min,
  select,
  selectAll,
  format,
  hsl,
  rgb,
  interpolateHsl,
  interpolateHslLong,
};
